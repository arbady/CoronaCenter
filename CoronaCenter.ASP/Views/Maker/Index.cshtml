@model IEnumerable<CoronaCenter.Model.Entities.Maker>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @item.Id
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
            <td>
                <a asp-controller="Maker"
                   asp-action="Update"
                   asp-route-id="@item.Id">
                    Modifier
                </a>|
                <a asp-controller="Maker"
                   asp-action="Delete"
                   asp-route-id="@item.Id"
                   class="delete-link">
                    Supprimer
                </a>
            </td>
        </tr>
}
    </tbody>
</table>
